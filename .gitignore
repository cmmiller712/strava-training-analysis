# macOS
.DS_Store

# Raw data (never commit)
data/raw/
data/raw/**
Raw Data/
Raw Data/**
*.zip

# Processed data — raw inputs ignored, aggregated outputs allowed
# runs_enriched.csv and weekly_model.csv are committed for Streamlit Cloud
data/processed/*.backup.csv

# Python / Jupyter
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.jupyter_config/
.ipython_dir/

# Secrets
.env

# Output data files (never commit raw CSVs)
outputs/*.csv

# FIT files (GPS + HR raw data)
*.fit

# Claude Code internals
.claude/